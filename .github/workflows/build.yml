name: Build iOS App

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 'latest-stable'

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Build for Simulator
        run: |
          xcodebuild build \
            -project SpectreIOS.xcodeproj \
            -scheme SpectreIOS \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty

      - name: Build for Device (Unsigned)
        if: github.event_name == 'workflow_dispatch'
        run: |
          xcodebuild build \
            -project SpectreIOS.xcodeproj \
            -scheme SpectreIOS \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            -derivedDataPath build/DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty

      - name: Create IPA
        if: github.event_name == 'workflow_dispatch'
        run: |
          mkdir -p build/Payload
          cp -r build/DerivedData/Build/Products/Release-iphoneos/SpectreIOS.app build/Payload/
          cd build && zip -r SpectreIOS.ipa Payload
          ls -la

      - name: Upload IPA
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: SpectreIOS-IPA
          path: build/SpectreIOS.ipa
          retention-days: 30

      - name: Upload Simulator App
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: SpectreIOS-Simulator
          path: build/DerivedData/Build/Products/Debug-iphonesimulator/SpectreIOS.app
          retention-days: 7
